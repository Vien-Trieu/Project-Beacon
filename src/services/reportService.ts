export type Report = {
    publicId: string;
    deviceSerial: string;
    salesOrder: string;
    status: string;
    filename: string;
    pdfUrl: string;
};

const mockReports: Record<string, Report> ={
    
}